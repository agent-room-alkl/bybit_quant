{
  "version": 2,
  "regions": ["sin1"],
  "crons": [
    {
      "path": "/api/tick",
      "schedule": "* * * * *"
    }
  ],
  "builds": [
    {
      "src": "api/tick.py",
      "use": "@vercel/python",
      "config": { "includeFiles": ["lib/**"], "maxLambdaSize": "250mb" }
    },
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": { "includeFiles": ["lib/**"], "maxLambdaSize": "250mb" }
    }
  ],
  "routes": [
    { "src": "/api/tick", "dest": "/api/tick.py" },
    { "src": "/(.*)", "dest": "/api/app.py" }
  ]
}
